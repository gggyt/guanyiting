代码运行：
	1.在电脑上运行redis(进入redis目录，执行redis-service.exe redis.windows.conf
	2.在idea中打开wei_account，运行
	3.进入acm_wei_account，执行npm install导入包，进入my-app2目录，执行npm start
	4.后端页面进入http://localhost:3000/login（管理员账号：gyt，密码：123456）